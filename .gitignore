# Move build artifacts
contracts/build/

# transient CLI output / any hidden json/err captured during development
# (NEVER commit these — sui --json wallet output can contain a recovery phrase)
/.*.json
/.*.err
*.tmp

# env-specific agent config (object ids are public, but per-deployment)
warden.config.json

# node / tooling
node_modules/
.DS_Store
*.log

# Python bytecode
__pycache__/
*.pyc
